[project]
requires = ["nodejs-24"]
project_type = "web"
template = "custom"

[dev]
build = ["pnpm", "install"]
run = ["sh", "-c", "pnpm run build && node server.js"]

[deploy]
build = ["sh", "scripts/deploy-build.sh"]
run = ["node", "server.js"]
